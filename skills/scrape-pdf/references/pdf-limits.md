# PDF limits

- Default max pages: **200** per file.
- Scanned PDFs without a text layer may return little or no text (OCR not in MVP).
- Title taken from PDF metadata when available, else filename stem.
